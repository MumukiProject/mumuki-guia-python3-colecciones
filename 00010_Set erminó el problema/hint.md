No es necesario modificar tooooda la clase `Biblioteca`, los métodos que deberíamos modificar son `__init__` y `agregar_libro`. Es importante recordar que:

* `set()` es un set vacío;
* los sets no entienden el mensaje `append`.